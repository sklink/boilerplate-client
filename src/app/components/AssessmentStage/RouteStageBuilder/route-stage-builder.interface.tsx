import { IFormOption } from '../../_core/_ui/forms.component';

export interface IRouteStageBuilderFormValues {
  routes: any[];
  notifications: IFormOption;
  enableNotifications: boolean;
  manualApproval: boolean;
}
